apiVersion: v1
kind: Service
metadata:
  name: league-frontend-service
  namespace: league-app
spec:
  selector:
    app: league-app
    tier: frontend
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 80
  type: LoadBalancer